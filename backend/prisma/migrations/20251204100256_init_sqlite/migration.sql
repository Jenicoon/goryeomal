-- Initial SQLite migration placeholder (restored)
-- This no-op migration unblocks prisma migrate deploy when the original file is missing.
-- If schema changes are required, add SQL statements here or run a fresh migration.

BEGIN;
-- no operations
COMMIT;
